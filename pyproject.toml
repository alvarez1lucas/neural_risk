[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neural-risk"
version = "0.1.0"
description = "ML-powered risk modeling with VaR, ES and hedging optimization"
requires-python = ">=3.8"
dependencies = []
