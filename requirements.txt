# ============================================================
# Neural Risk Engine - requirements.txt
# Verificado contra los imports REALES del codigo (grep -r), no a ojo.
# ============================================================

# --- Core (el proyecto no arranca sin esto) ---
numpy>=1.23.0
pandas>=1.5.0
scipy>=1.9.0
scikit-learn>=1.2.0
PyYAML>=6.0

# --- Time Series & Statistics (usadas por feature_engineering.py, data_processor.py) ---
statsmodels>=0.13.0
arch>=5.3.0
hmmlearn>=0.2.7

# --- Deep Learning (ensemble_trainer, lstm_transformer, anomaly_detection, risk_model) ---
torch==2.5.1

# --- Gradient Boosting (classic.py, causal_strategy.py) ---
xgboost>=1.7.0

# --- Conectores de datos reales (loaders.py, run_data_fetcher.py con use_mock=false) ---
ccxt>=2.0.0
yfinance>=0.2.0
requests>=2.28.0

# ============================================================
# OPCIONALES -- alcance futuro de la libreria (RL/Copula), NO
# conectados al engine activo hoy. Descomentar solo si se activan.
# ============================================================
# stable-baselines3>=2.0.0
# copulae>=0.7.0

# ============================================================
# DEV TOOLING (opcional, no lo usa el motor)
# ============================================================
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.1.0
# flake8>=6.0.0
# mypy>=1.0.0
# python-dotenv>=0.21.0
# loguru>=0.7.0
# tqdm>=4.65.0
# jupyter>=1.0.0
# jupyterlab>=3.5.0
# ipywidgets>=8.0.0
# sqlalchemy>=2.0.0